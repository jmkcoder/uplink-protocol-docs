import { generateMetadata } from "@/lib/metadata";
import { Metadata } from "next";

export const metadata: Metadata = generateMetadata(
  'Contact JmkCoder',
  'Get in touch with JmkCoder for support, feature requests, or partnership inquiries.',
  '/contact-us'
);
