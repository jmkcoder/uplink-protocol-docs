import { generateMetadata } from "@/lib/metadata";
import { Metadata } from "next";

export const metadata: Metadata = generateMetadata(
  'Privacy Policy - Uplink Protocol',
  'Our commitment to protecting your privacy and information while using Uplink Protocol services.',
  '/privacy-policy'
);
