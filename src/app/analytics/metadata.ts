import { generateMetadata } from "@/lib/metadata";
import { Metadata } from "next";

export const metadata: Metadata = generateMetadata(
  'Analytics Integration - Uplink Protocol',
  'Learn how to track usage and performance metrics with Uplink Protocol analytics capabilities.',
  '/analytics'
);
